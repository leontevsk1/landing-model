# Модель демонстрирующая работу решения в режиме посадки

## Цель

Смоделировать посадку БПЛА по сигналам с маяка

## Принцип реализации

### БПЛА

- моделируется как **точка** на плоскости;

- имеет **направленную антенну** (угол приёма ≈ 45°);

- управляется в двух координатах *(X, Y)*, без 3D;

- логика полёта — **наш адаптированный курсово-глиссадный алгоритм**.

### Маяк

- моделируется как **точка** с восемью секторами излучения по 45°;

- активирует сектора **поочерёдно** (TDM, цикл 12.8 мс);

- вычисляет и передаёт мощность сигнала в направлении на БПЛА.

### Сценарии

- отдельный модуль для параметров (ветер, шум, стартовые координаты);

- реализуем **в последнюю очередь**;

- если не успеваем — описываем логику и оставляем заглушки.

### Визуализация

- плоская 2D-анимация: маяк, сектора, траектория БПЛА;

- matplotlib / FuncAnimation (или аналог);

- вывод в `.gif` / `.mp4` для презентации.

### Правила работы с репозиторием

- **Не работаем над одним и тем же файлом одновременно.**  
  Перед началом правок — обнови репозиторий:
  
  ```bash
  git fetch origin; and git rebase origin/master
  ```

- Подробнее правила описаны в соответствующем файле в корне проекта.

**Задачи в README пишем вместе и обновляем их статус перед коммитами**

## Структура

### Тут пишем общую структуру и документируем функции чтобы быстрее понимать что делать в задачах

**Базовая структура проекта:**

```bash
.
├──.git
├──.gitignore 
├── environment.lock.yml - конфиг окружения
├── README.md
├── export_data/ - директория для экспорта анимаций
├── src/
│   ├── beacon.py - маяк
│   ├── dron.py   - дрон
│   ├── main.py   - посадка в идеале
│   ├── scenes.py - сценарии посадки
│   └── visual.py - визуализация
└── как-ведем-гит.md
```

## Задачи

1) Синхронизировать git-репозиторий.

2) Создать каркас проекта (папки + пустые модули).



## Мысли вслух

Как я понимаю нам нужно реализовать две этих блок-схемы, только углубить их, некоторые блоки представляют из себя подпрограммы, и думаю первым этапом мы должны будем теоритически раскрыть каждый модуль

- Для маяка:
  
  ![prog2.jpg](/home/artem/Documents/_VAULTS/01_dev-vault/BPLA/prog2.jpg)
  
  

- Для дрона:        ![BPLA.jpg](/home/artem/Documents/_VAULTS/01_dev-vault/BPLA/BPLA.jpg)
